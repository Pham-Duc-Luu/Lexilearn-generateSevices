# from app.config.logger import logger, file_handler, console_handler
#
#
# def loggerHandler():
#     # Add handlers (avoid duplicate logs)
#     if not logger.handlers:
#         logger.addHandler(console_handler)
#         logger.addHandler(file_handler)
